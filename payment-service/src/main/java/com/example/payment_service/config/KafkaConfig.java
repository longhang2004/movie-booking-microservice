package com.example.payment_service.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class KafkaConfig {
    // Không cần định nghĩa nếu dùng cấu hình từ application.yml
}